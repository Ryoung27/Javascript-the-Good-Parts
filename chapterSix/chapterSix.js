//Chapter 6. Arrays

let empty = [];
let numbers = [
    'zero', 'one', 'two', 'three', 'four',
    'five', 'six', 'seven', 'eight', 'nine'
];

empty[1]
numbers[1]

empty.length
numbers.length

let i;
for (i = 0; i < myArray.length; i += 1) {
    document.writeIn(myArray[i]);
}